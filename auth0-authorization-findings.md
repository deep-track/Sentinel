Critical production finding: navigating to https://sentinel.deeptrack.io/api/auth/token while authenticated was redirected to the Auth0 `/v2/logout` endpoint. The repository contains `app/api/auth/[auth0]/route.ts`, whose generic GET handler performs logout for any `/api/auth/*` path, while the intended token bridge is incorrectly named `app/api/auth/token/routes.ts` (plural `routes.ts`, not Next.js `route.ts`). Therefore `/api/auth/token` is not a token endpoint; it is caught by the generic route and logs the user out. This must be corrected before the client-side Convex auth bridge can work reliably.

2026-08-26 production diagnosis: Convex logs showed repeated failures for `authz:currentAccess` with `Could not find public function for 'authz:currentAccess'`. The stable replacement was committed as `56706cd`, changing the platform gate to `auth.currentAccess` and removing the obsolete duplicate module. Convex backend deployment succeeded at 00:12; Vercel must serve commit `56706cd` before another login test.
