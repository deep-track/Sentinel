User successfully deployed from a temporary `backend/package.json` while targeting production `insightful-lark-924`. Convex reported: no indexes deleted, functions uploaded, TypeScript passed, schema validation complete, rateLimiter installed, and deployment finalized. However, the production Dashboard Functions view currently shows no `authz` or `memberships` module; searching for `authz` returns no functions. Local `backend/convex/authz.ts` does export `currentAccess` as a Convex query, so the dashboard inventory requires further verification (possibly stale UI/component selection or deployment source mismatch).
